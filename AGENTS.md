## Agent skills

### Issue tracker

本專案使用本機 Markdown issue tracker，規格與議題放在 `.scratch/`。See `docs/agents/issue-tracker.md`.

### Triage labels

使用預設五種 triage 標籤，以 issue 的 `Status:` 記錄狀態。See `docs/agents/triage-labels.md`.

### Domain docs

本專案為 single-context，使用根目錄 `CONTEXT.md` 與按需建立的 `docs/adr/`。See `docs/agents/domain.md`.
